apiVersion: v1
kind: Service
metadata:
  name: swkoo-frontend
  namespace: swkoo
  labels:
    app: swkoo-frontend
spec:
  selector:
    app: swkoo-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
  type: ClusterIP
