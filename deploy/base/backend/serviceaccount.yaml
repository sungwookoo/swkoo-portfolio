apiVersion: v1
kind: ServiceAccount
metadata:
  name: swkoo-backend
  namespace: swkoo
  labels:
    app: swkoo-backend
# Bound to per-user Role/RoleBinding generated by templates.ts on each
# /deploy/register — grants secrets CRUD + deployments patch in the
# user-<login> namespace, used by the EnvService to manage runtime env vars.
