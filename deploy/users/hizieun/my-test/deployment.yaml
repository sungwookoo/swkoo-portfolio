# Generated by swkoo.kr — do not edit; re-deploy from https://swkoo.kr/deploy to regenerate.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-test
  namespace: user-hizieun
  labels:
    app: my-test
    swkoo.kr/user: hizieun
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-test
  template:
    metadata:
      labels:
        app: my-test
        swkoo.kr/user: hizieun
    spec:
      containers:
        - name: my-test
          image: ghcr.io/hizieun/my-test:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
