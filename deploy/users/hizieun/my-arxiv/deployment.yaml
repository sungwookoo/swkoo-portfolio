# Generated by swkoo.kr — do not edit; re-deploy from https://swkoo.kr/deploy to regenerate.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-arxiv
  namespace: user-hizieun
  labels:
    app: my-arxiv
    swkoo.kr/user: hizieun
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-arxiv
  template:
    metadata:
      labels:
        app: my-arxiv
        swkoo.kr/user: hizieun
    spec:
      containers:
        - name: my-arxiv
          image: ghcr.io/hizieun/my-arxiv:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            # Populated by swkoo.kr/deploy → Environment variables panel.
            # optional:true so the Pod boots before the user sets anything.
            - secretRef:
                name: my-arxiv-env
                optional: true
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
