# Generated by swkoo.kr — do not edit; re-deploy from https://swkoo.kr/deploy to regenerate.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextjs-sample
  namespace: user-sw-koo
  labels:
    app: nextjs-sample
    swkoo.kr/user: sw-koo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nextjs-sample
  template:
    metadata:
      labels:
        app: nextjs-sample
        swkoo.kr/user: sw-koo
    spec:
      containers:
        - name: nextjs-sample
          image: ghcr.io/sw-koo/nextjs-sample:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            # Populated by swkoo.kr/deploy → Environment variables panel.
            # optional:true so the Pod boots before the user sets anything.
            - secretRef:
                name: nextjs-sample-env
                optional: true
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
